<?php
//no if, deve haver um espaço depois do fechamento do parenteses e antes da chave que deve ser na mesma linha;
if($_GET['var'] != null) { //as constantes, como null, true e false devem ser em letras minúsculas;
    echo $_GET['var'];
}
//tag de fechamento do php não deve ser usado em códigos que possuem apenas PHP;
//arquivos devem terminar com uma única linha em branco;
